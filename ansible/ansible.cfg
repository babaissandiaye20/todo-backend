# Reglages Ansible (evite de repeter des options a chaque commande).
[defaults]
host_key_checking = False   # pas de question "trust this host?" (vital en CI/CD)

[privilege_escalation]
become = True               # droits admin (sudo) par defaut
